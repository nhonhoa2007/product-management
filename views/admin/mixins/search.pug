mixin search(keyword)
    form(id="form-search")
        .input-group
            input(
                type="text",
                placeholder="Nhập từ khóa...",
                value=keyword,
                name="keyword",
                class="form-control"
            )
            .input-group-append
                button(
                    class="btn btn-success",
                    type="submit"
                ) Tìm kiếm
